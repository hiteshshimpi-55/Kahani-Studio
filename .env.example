# Copied into containers via compose environment for now.
# Add LLM/TTS keys here later — not required for base setup.
DATABASE_URL=postgresql+asyncpg://kissa:kissa@postgres:5432/kissa
REDIS_URL=redis://redis:6379/0
DATA_DIR=/data
